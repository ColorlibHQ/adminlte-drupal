{#
/**
 * @file
 * AdminLTE 4 implementation of a single local task (admin tab).
 *
 * The Bootstrap `nav-item` / `nav-link` / `active` classes are added in
 * adminlte_preprocess_menu_local_task().
 *
 * @see template_preprocess_menu_local_task()
 * @see adminlte_preprocess_menu_local_task()
 */
#}
<li{{ attributes }}>{{ link }}</li>
