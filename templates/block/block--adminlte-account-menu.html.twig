{#
/**
 * @file
 * Minimal wrapper for the account-menu block.
 *
 * The block is placed inside the AdminLTE navbar's <ul class="navbar-nav">, and
 * menu--account.html.twig emits the navbar dropdown <li> itself — so this block
 * template deliberately renders no wrapper element or heading.
 *
 * @see menu--account.html.twig
 * @see adminlte_preprocess_menu__account()
 */
#}
{{ title_prefix }}
{{ title_suffix }}
{{ content }}
